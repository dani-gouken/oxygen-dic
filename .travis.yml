language: php

php:
- 7.2
- 7.3

install:
- travis_retry composer install --no-interaction --no-suggest

script:
- composer test -- --coverage-clover=coverage.xml

branches:
  only:
  - master
  - dev
cache:
  directories:
  - $HOME/.composer/cache